import { View, Text } from 'react-native';

export default function OrganizationScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Organization Home</Text>
        </View>
    );
}